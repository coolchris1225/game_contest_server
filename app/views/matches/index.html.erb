<% provide(:title, 'All Matches') %>
<h1>Matches</h1>

<ul>
<% @matches.each do |f| %>
  <% if f.manager_id == @contest.id %>
    <li><%= link_to f.id, match_path(f) %></li>
  <% end %>
<% end %>
</ul>